[project]
name = "admin-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1.5",
    "psycopg[binary,pool]>=3.2.4",
]

[dependency-groups]
dev = [
    "ruff>=0.9.3",
]
